{% extends 'prosgaBundle::template.html.twig' %}

{% block content %}
    <h2>Ver Actividad</h2>
{% endblock %}

{% block body_content -%}

    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ entity.fechaActividad|date('Y-m-d H:i') }}</td>
            </tr>
            <tr>
                <th>Observación</th>
                <td>{{ entity.observacion }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('actividad') }}">
            Volver a la lista
        </a>
    </li>
    <li>
        <a href="{{ path('actividad_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
