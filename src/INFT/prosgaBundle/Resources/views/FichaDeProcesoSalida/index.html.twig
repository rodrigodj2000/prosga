{% extends 'prosgaBundle::template.html.twig' %}

{% block content %}
    <h2>Salidas de Fichas de Procesos</h2>
{% endblock %}

{% block body_content %} 
    <table class="table table-striped">
        <thead>
            <tr>
                <!--th> Id </th--!>
                <th> Salida del Proceso </th>
                <th> Valores de Salida </th>                
                <th> Observaciones </th>
                <th> Acciones </th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <!--td><a href="{{ path('fichadeprocesosalida_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td--!>
                <td><a href="{{ path('fichadeproceso_show', { 'id': entity.fichaDeProceso.id }) }}">{{ entity.fichaDeProceso }}</a></td>
                <td>{{ entity.valoresDeSalida }}</td>                
                <td>{{ entity.observacion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('fichadeprocesosalida_show', { 'id': entity.id }) }}">Ver</a>
                    </li>
                    <li>
                        <a href="{{ path('fichadeprocesosalida_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('fichadeprocesosalida_new') }}">
               Agregar Nuevo
            </a>
        </li>
    </ul>
    {% endblock %}
